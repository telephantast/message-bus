services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'

  rabbitmq:
    image: rabbitmq:4-management-alpine
    restart: always
    ports:
      - '5672:5672'
      - '15672:15672'
